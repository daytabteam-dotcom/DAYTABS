{
  "$schema": "./railway.schema.json",
  "build": {
    "buildCommand": "pnpm run build:prod"
  },
  "deploy": {
    "startCommand": "sh -c \"node ./scripts/wait-for-db.js && pnpm --filter @workspace/db run push && pnpm run start\"",
    "healthcheckPath": "/api/healthz"
  }
}
