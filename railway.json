{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "NODE_ENV=production pnpm --filter @workspace/api-server run build && BASE_PATH=/ pnpm --filter @workspace/landing run build && BASE_PATH=/panel/ pnpm --filter @workspace/daytabs run build"
  },
  "deploy": {
    "startCommand": "node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "healthcheckPath": "/api/healthz"
  }
}
